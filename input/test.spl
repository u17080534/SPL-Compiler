string wrong;
string correct;
bool eqv;
wrong = "wrong";
correct = "correct";
eqv = F;
eqv = "correct";
hello;

num i;
num a;

for (i = 0; i < a; i = add(i, 1))
{
    output (correct);

    for (i = 0; i < a; i = add(i, 1))
    {
       output (correct)
    }
};

num other;

proc guess
{
    num x;
    num other;
    num answer;

    x = 9;
    x = add(sub(mult(1,x),8),sub(8, mult(1,x)));

    while(not or(and(not eqv, eq(answer, other)), or(eqv, not eq(answer, other))))
    {

        other = 7;

        input (answer);

        eqv = eq(answer, other)


    };

    if(eqv) then
    {
        num s;
        num t;
        t = s;
        if(eq(answer, other)) then
        {
          answer = 4;
          output (correct)
        };

        output (answer)
    }
    else
    {

        output (wrong);
        answer = 5
    }
}

proc hello
{
    string h;
    h = "hello";
    hello2;
    guess;

    proc hello2
    {
        output(h)
    }
}
