proc checnuber{
	num zero;
	zero= 0;
	m=n;
	if ((m<zero))
		then{ m=mult(m,-1)};
	while ((m<zero)){m=sub(m,2);
	if(eq(m,zero))
		then{r="even"}
		else{r="odd"}
	};

	proc xx{
		output (zero)
		} 
	proc yy
	{ 
		proc xt
		{
			output (zero)
		} 
		proc pp
		{
			output (zero)
		} 
		proc yh
		{
			proc pv
			{
				output (zero)
			}
		}
	}
}